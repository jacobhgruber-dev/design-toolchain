# Supervisor UI/UX Updates

This file logs the fixes applied to bridge the gap between installed design tools and the supervisor's usage of them.

1. **Root Cause Resolved**: Added explicit `TRIGGER` blocks to `SKILL.md` descriptions so the supervisor can discover them. Added a structured UI/UX triage workflow to `supervisor.md`.
2. **Visual Loop**: Formalized the multimodal worker delegation pattern using Playwright.
3. **Open Design**: Enabled the `open-design` MCP server in `opencode.json`.
